Please add the user that is running the web server to the sudoers file, so it can run the quota checking using root.
I added this line to /etc/sudoers:
apache  ALL=(ALL)       NOPASSWD: /usr/bin/quota
